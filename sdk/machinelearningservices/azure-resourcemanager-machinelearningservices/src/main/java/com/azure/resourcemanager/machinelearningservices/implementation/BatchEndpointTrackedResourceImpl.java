// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.machinelearningservices.implementation;

import com.azure.core.http.rest.Response;
import com.azure.core.management.Region;
import com.azure.core.management.SystemData;
import com.azure.core.util.Context;
import com.azure.resourcemanager.machinelearningservices.fluent.models.BatchEndpointInner;
import com.azure.resourcemanager.machinelearningservices.fluent.models.BatchEndpointTrackedResourceInner;
import com.azure.resourcemanager.machinelearningservices.models.BatchEndpoint;
import com.azure.resourcemanager.machinelearningservices.models.BatchEndpointTrackedResource;
import com.azure.resourcemanager.machinelearningservices.models.EndpointAuthKeys;
import com.azure.resourcemanager.machinelearningservices.models.PartialBatchEndpoint;
import com.azure.resourcemanager.machinelearningservices.models.PartialBatchEndpointPartialTrackedResource;
import com.azure.resourcemanager.machinelearningservices.models.ResourceIdentity;
import java.util.Collections;
import java.util.Map;

public final class BatchEndpointTrackedResourceImpl
    implements BatchEndpointTrackedResource,
        BatchEndpointTrackedResource.Definition,
        BatchEndpointTrackedResource.Update {
    private BatchEndpointTrackedResourceInner innerObject;

    private final com.azure.resourcemanager.machinelearningservices.MachineLearningServicesManager serviceManager;

    public String id() {
        return this.innerModel().id();
    }

    public String name() {
        return this.innerModel().name();
    }

    public String type() {
        return this.innerModel().type();
    }

    public String location() {
        return this.innerModel().location();
    }

    public Map<String, String> tags() {
        Map<String, String> inner = this.innerModel().tags();
        if (inner != null) {
            return Collections.unmodifiableMap(inner);
        } else {
            return Collections.emptyMap();
        }
    }

    public ResourceIdentity identity() {
        return this.innerModel().identity();
    }

    public String kind() {
        return this.innerModel().kind();
    }

    public BatchEndpoint properties() {
        BatchEndpointInner inner = this.innerModel().properties();
        if (inner != null) {
            return new BatchEndpointImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    public SystemData systemData() {
        return this.innerModel().systemData();
    }

    public Region region() {
        return Region.fromName(this.regionName());
    }

    public String regionName() {
        return this.location();
    }

    public BatchEndpointTrackedResourceInner innerModel() {
        return this.innerObject;
    }

    private com.azure.resourcemanager.machinelearningservices.MachineLearningServicesManager manager() {
        return this.serviceManager;
    }

    private String endpointName;

    private String resourceGroupName;

    private String workspaceName;

    private PartialBatchEndpointPartialTrackedResource updateBody;

    public BatchEndpointTrackedResourceImpl withExistingWorkspace(String resourceGroupName, String workspaceName) {
        this.resourceGroupName = resourceGroupName;
        this.workspaceName = workspaceName;
        return this;
    }

    public BatchEndpointTrackedResource create() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getBatchEndpoints()
                .createOrUpdateWithResponse(
                    endpointName, resourceGroupName, workspaceName, this.innerModel(), Context.NONE)
                .getValue();
        return this;
    }

    public BatchEndpointTrackedResource create(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getBatchEndpoints()
                .createOrUpdateWithResponse(endpointName, resourceGroupName, workspaceName, this.innerModel(), context)
                .getValue();
        return this;
    }

    BatchEndpointTrackedResourceImpl(
        String name, com.azure.resourcemanager.machinelearningservices.MachineLearningServicesManager serviceManager) {
        this.innerObject = new BatchEndpointTrackedResourceInner();
        this.serviceManager = serviceManager;
        this.endpointName = name;
    }

    public BatchEndpointTrackedResourceImpl update() {
        this.updateBody = new PartialBatchEndpointPartialTrackedResource();
        return this;
    }

    public BatchEndpointTrackedResource apply() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getBatchEndpoints()
                .updateWithResponse(endpointName, resourceGroupName, workspaceName, updateBody, Context.NONE)
                .getValue();
        return this;
    }

    public BatchEndpointTrackedResource apply(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getBatchEndpoints()
                .updateWithResponse(endpointName, resourceGroupName, workspaceName, updateBody, context)
                .getValue();
        return this;
    }

    BatchEndpointTrackedResourceImpl(
        BatchEndpointTrackedResourceInner innerObject,
        com.azure.resourcemanager.machinelearningservices.MachineLearningServicesManager serviceManager) {
        this.innerObject = innerObject;
        this.serviceManager = serviceManager;
        this.endpointName = Utils.getValueFromIdByName(innerObject.id(), "batchEndpoints");
        this.resourceGroupName = Utils.getValueFromIdByName(innerObject.id(), "resourceGroups");
        this.workspaceName = Utils.getValueFromIdByName(innerObject.id(), "workspaces");
    }

    public BatchEndpointTrackedResource refresh() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getBatchEndpoints()
                .getWithResponse(endpointName, resourceGroupName, workspaceName, Context.NONE)
                .getValue();
        return this;
    }

    public BatchEndpointTrackedResource refresh(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getBatchEndpoints()
                .getWithResponse(endpointName, resourceGroupName, workspaceName, context)
                .getValue();
        return this;
    }

    public EndpointAuthKeys listKeys() {
        return serviceManager.batchEndpoints().listKeys(endpointName, resourceGroupName, workspaceName);
    }

    public Response<EndpointAuthKeys> listKeysWithResponse(Context context) {
        return serviceManager
            .batchEndpoints()
            .listKeysWithResponse(endpointName, resourceGroupName, workspaceName, context);
    }

    public BatchEndpointTrackedResourceImpl withRegion(Region location) {
        this.innerModel().withLocation(location.toString());
        return this;
    }

    public BatchEndpointTrackedResourceImpl withRegion(String location) {
        this.innerModel().withLocation(location);
        return this;
    }

    public BatchEndpointTrackedResourceImpl withProperties(BatchEndpointInner properties) {
        this.innerModel().withProperties(properties);
        return this;
    }

    public BatchEndpointTrackedResourceImpl withTags(Map<String, String> tags) {
        if (isInCreateMode()) {
            this.innerModel().withTags(tags);
            return this;
        } else {
            this.updateBody.withTags(tags);
            return this;
        }
    }

    public BatchEndpointTrackedResourceImpl withIdentity(ResourceIdentity identity) {
        if (isInCreateMode()) {
            this.innerModel().withIdentity(identity);
            return this;
        } else {
            this.updateBody.withIdentity(identity);
            return this;
        }
    }

    public BatchEndpointTrackedResourceImpl withKind(String kind) {
        if (isInCreateMode()) {
            this.innerModel().withKind(kind);
            return this;
        } else {
            this.updateBody.withKind(kind);
            return this;
        }
    }

    public BatchEndpointTrackedResourceImpl withProperties(PartialBatchEndpoint properties) {
        this.updateBody.withProperties(properties);
        return this;
    }

    private boolean isInCreateMode() {
        return this.innerModel().id() == null;
    }
}
